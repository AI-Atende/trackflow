POSTGRES_USER=trackflow_user
POSTGRES_PASSWORD=trackflow_pass
POSTGRES_DB=trackflow_db
DATABASE_URL="postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@database:5432/${POSTGRES_DB}"

# If using System User approach, put the long-lived token here (optional)
META_SYSTEM_USER_TOKEN=""

# If using OAuth, set these
META_APP_ID=""
META_APP_SECRET=""
META_REDIRECT_URI="http://localhost:4000/api/meta/oauth/callback"

PORT=4000

# JWT secret for token signing (change in production)
JWT_SECRET="change-me"
